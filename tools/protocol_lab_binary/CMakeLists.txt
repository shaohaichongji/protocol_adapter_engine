if(PAE_BUILD_PROTOCOL_LAB_BINARY_PUBLIC_H1)
    add_library(pae_protocol_lab_binary_public_h1 STATIC public_binary_decode.cpp public_binary_decode.h)
    target_include_directories(pae_protocol_lab_binary_public_h1 PUBLIC "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>")
    target_link_libraries(pae_protocol_lab_binary_public_h1 PUBLIC PAE::pae PRIVATE pae::project_options)
    set_target_properties(pae_protocol_lab_binary_public_h1 PROPERTIES CXX_VISIBILITY_PRESET hidden VISIBILITY_INLINES_HIDDEN YES)
endif()

if(PAE_BUILD_PROTOCOL_LAB_BINARY_MATERIALIZER)
add_library(pae_protocol_lab_binary_materializer STATIC candidate_materializer.cpp candidate_materializer.h owned_description.cpp owned_description.h binary_admission.cpp binary_admission.h prepared_binary.cpp prepared_binary.h prepared_binary_encode.cpp resource_budget.cpp resource_budget.h)
target_include_directories(pae_protocol_lab_binary_materializer PUBLIC "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>")
target_link_libraries(pae_protocol_lab_binary_materializer PUBLIC pae_host_endpoint pae_config_compiler PRIVATE pae::project_options)
if(PAE_BUILD_TESTING)
    target_compile_definitions(pae_protocol_lab_binary_materializer PUBLIC PAE_PROTOCOL_LAB_BINARY_TEST_HOOKS=1)
endif()
set_target_properties(pae_protocol_lab_binary_materializer PROPERTIES CXX_VISIBILITY_PRESET hidden VISIBILITY_INLINES_HIDDEN YES)
endif()
